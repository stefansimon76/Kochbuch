<div class="card-body">
    <form method="POST" action="register" onsubmit="return validateRegisterForm()">
        <div class="section__content">
            <h4 class="card-title">Registrieren</h4>

            {{#errors}}
                <div id="server_errors" class="card alert-danger p-1 mb-2">
                    <p>{{.}}</p>
                </div>
            {{/errors}}
            <div id="client_errors" class="card alert-danger p-1 mb-2" hidden="hidden">
            </div>

            <div class="form-group">
                <label for="realname" hidden="hidden"></label>
                <input class="form-control" id="realname" type="text" name="realname"
                       placeholder="Benutzername (öffentlich)"
                       value="{{realname}}" minlength="" maxlength="50">
            </div>
            <div class="form-group">
                <label for="loginname" hidden="hidden"></label>
                <input class="form-control" id="loginname" type="text" name="loginname"
                       placeholder="Anmeldename (geheim)"
                       value="{{loginname}}" minlength="5" maxlength="20">
            </div>
            <div class="form-group">
                <label for="password" hidden="hidden"></label>
                <input class="form-control" id="password" type="password" name="password"
                       placeholder="Passwort"
                       value="" minlength="8" maxlength="20">
                <input type="hidden" id="md5pw1" name="md5pw1" value="" />
            </div>
            <div class="form-group">
                <label for="passwordRepeat" hidden="hidden"></label>
                <input class="form-control" id="passwordRepeat" type="password" name="passwordRepeat"
                       placeholder="Password wiederholen"
                       value="" minlength="8" maxlength="20">
                <input type="hidden" id="md5pw2" name="md5pw2" value="" />
            </div>
            <div class="form-group">
                <label for="email" hidden="hidden"></label>
                <input class="form-control" id="email" type="email" name="email"
                       placeholder="E-Mail Adresse"
                       value="{{email}}" minlength="5" maxlength="200">
            </div>
            <div class="form-check mb-2">
                <input class="form-check-input" type="checkbox" name="terms" id="terms">
                <label class="form-check-label" for="terms">Akzeptiere die <a class="card-link" href="terms" target="_blank">Nutzungsbedingungen</a></label>
            </div>

            <div class="form-group">
                <button class="btn btn-primary" type="submit">Registrieren</button>
            </div>

            Ich habe bereits einen Account und möchte mich damit <a class="card-link" href="/">anmelden</a>

        </div>
    </form>
</div>